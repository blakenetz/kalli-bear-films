import { useState, useEffect } from "preact/hooks";
import PropTypes from "prop-types";

function Home(props) {
  useEffect(() => {}, []);

  return (
    <section class="home">
      <h1>HOME</h1>
    </section>
  );
}

export default Home;
